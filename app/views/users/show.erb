<h1><%= @user.username %>'s Reviews</h1>

<h2>You have <%= @user.reviews.size %> Book(s) logged!</h2>

<h3>Review(s):</h3>
  <% @user.reviews.each do |review| %>
    <h4><a href="/review/<%= review.id %>"> <%= review.book.name %></h4></a>
  <% end %>

<br>

<h2>Add a new Review!</h2>

<form action="/review" method="POST">
  <label for="book_name">Book Name:</label>
    <select name="book_name">
      <% @books.each do |book| %>
        <option value="<%= book.name %>"><%= book.name %></option>
      <% end %>
    </select>
  Or, enter a new book
    <input type="text" name="new_book_name">
  <label for="length_of_visit">Length of Visit:</label>
    <input type="text" name="length_of_visit">
  <button type="submit" value="submit">Add Trip</button>
</form>